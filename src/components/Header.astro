---
// Header component
---

<header class="site-header">
  <div class="container">
    <nav class="nav">
      <a href="/" class="logo">
        <img src="/images/logos/logo-horizontal.png" alt="Tangara Studio" class="logo-img" />
      </a>
      
      <div class="nav-links">
        <a href="/">Blog</a>
        <a href="/tags">Tags</a>
        <a href="https://tangara.studio" target="_blank" rel="noopener">Web</a>
      </div>
    </nav>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--color-border);
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(10px);
    background: rgba(255, 255, 255, 0.95);
  }
  
  /* Modo oscuro deshabilitado
  @media (prefers-color-scheme: dark) {
    .site-header {
      background: rgba(10, 10, 10, 0.95);
    }
  }
  */
  
  .nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .logo {
    text-decoration: none;
    display: flex;
    align-items: center;
  }
  
  .logo-img {
    height: 80px;
    width: auto;
    transition: transform 0.2s ease;
  }
  
  .logo:hover .logo-img {
    transform: scale(1.05);
  }
  
  .nav-links {
    display: flex;
    gap: 2rem;
    align-items: center;
  }
  
  .nav-links a {
    color: var(--color-text);
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s;
  }
  
  .nav-links a:hover {
    color: var(--color-primary);
  }
  
  @media (max-width: 768px) {
    .nav-links {
      gap: 1rem;
    }
  }
</style>
